如何使用配置好的Style?

首先，拿已經改好的保險Table當作模板(在insurance_overview資料夾裡面)，將你要改的Table一一對應到該有的位置。

通常需要改的地方：
1.以保險table 116-125行舉例，更改你的TableHead標題。
一個<TableCell></TableCell>代表一個標題，所以你的<TableHead></TableHead>標幾個標題，<TableBody></TableBody>裡就要有幾個<TableCell></TableCell>。
2.保險table第100行，colSpan代表那一行跨了幾個欄位，通常colSpan裡的數字改成【你的欄位數】-【1】
3.保險table 129-153行全部改成你的資料。
4.其他的就是換成你的函數方法(CRUD的方法等)。


再來是Dialog也配置好css了，可以參考insurance_overview中的insuranceAdd.tsx檔案。
路徑和會用到的元件皆放在各個style最上方註解起來了，可以直接複製去用。
style的ts檔案中也有註解這個style是幹嘛用的

盡量把東西都套ts檔這樣以後比較好改。

table可以暫時讓它寬度是100%沒關係，因為其他部分還沒做好，先佔據整個頁面沒關係。

如果程式會報錯記得安裝 npm install @fontsource/public-sans 因為我改了字體。

⭐⭐所有有用Dialog的地方，麻煩讓Dialog都這樣⭐⭐：
<Dialog open={open} onClose={onClose} maxWidth="sm" fullWidth PaperProps={{ sx: { padding: 3 } }}>